// Extends Vitest's `expect` with DOM matchers (toBeInTheDocument, toHaveTextContent, …)
// and registers automatic cleanup of the rendered DOM between tests.
import '@testing-library/jest-dom/vitest';
